# Webflux Module

## Available Scripts

### Building Application

```shell
cd $(git rev-parse --show-toplevel) && \
  ./gradlew :webflux:clean \
            :webflux:build
```

### Running Application

```shell
cd $(git rev-parse --show-toplevel) && \
  ./gradlew webflux:bootRun
```
